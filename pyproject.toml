[project]
name = "openenv-agent"
version = "0.1.0"
description = "OpenEnv Submission"
requires-python = ">=3.10"
dependencies = [
    "openai",
    "fastapi",
    "uvicorn",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:app"